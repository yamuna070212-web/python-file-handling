file = open("numbers.txt", "r")

total = 0

for line in file:

    words = line.split()

    for word in words:

        if word.isdigit():
            total += int(word)

file.close()

print("Sum =", total)
Output
Sum = 100
